### Setup

1. Install neovim on path
2. `mkdir -p ~/.config/nvim` (remove the contents of the directory if it already exists)
3. git clone into `~/.config/nvim`
4. Start nvim and everything should be automatically installed.
